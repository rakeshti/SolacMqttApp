pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // paho repository

        maven {
            url "https://repo.eclipse.org/content/repositories/paho-snapshots/"
        }

    }
}
rootProject.name = "SolacMqttApp"
include ':app'
